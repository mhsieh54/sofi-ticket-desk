# Handoff — 2026-05-11

## Session 1: Project bootstrap

### What we did
- Created folder structure: `data/`, `data/comps/`, `strategy/`, `scripts/`, `dashboard/`, `handoffs/`
- Rewrote `CLAUDE.md` — lean, stable-rules-only operator manual (fees, core rules, SELL/ATTEND/CLIENT/KEEP categorization, file conventions, commands, never-rules)
- Created `README.md` as the project hub — points to data layout, run commands, and per-event strategy files
- Reset `data/inventory.json` to empty skeleton: `{"positions": []}`
- Initialized git (`git init`), added `.gitignore` (excludes `node_modules/`, `.env`, `*.log`). No commits made yet.

### Decisions made
- Found pre-existing `CLAUDE.md` and a populated `data/inventory.json` (16 positions — sold + active) left over from earlier work. Confirmed with Mark to overwrite both, no backups kept. The old inventory data is gone.
- `sofi-ticket-manager.jsx` (53KB React artifact) still sits at project root — not touched this session. This is the source for the inventory migration.
- Categorization scheme finalized as exactly one tag per position: SELL / ATTEND / CLIENT / KEEP.

### What's next (session 2)
- Migrate real inventory data out of `sofi-ticket-manager.jsx` into `data/inventory.json`, applying true-cost calculation and SELL/ATTEND/CLIENT/KEEP tags per position.
- Stand up `data/sold.json` and `data/journal.md` for realized sales.
- Build first per-event strategy file(s) in `strategy/`.
- Decide on initial git commit once inventory migration lands.
