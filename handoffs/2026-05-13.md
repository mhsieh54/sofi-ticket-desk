# Handoff — 2026-05-13

## Session 3: Decisions, sold/active split, cleanup

Scope: resolve the 3 open decisions from session 2, split sold/active data, clean up stale files, define the Bruno Mars Oct 7 strategy. Dashboard build deferred to its own session.

### Decisions confirmed by Mark
- **BTS pricing ceiling: $625/ticket confirmed.** The conflicting $700 roadmap figure is superseded. Updated `strategy/bts-arirang-sep-5.md`.
- **Rams vs Bills: ATTEND, not SELL.** Was `decision_pending` in the source data; Mark confirmed keep & attend despite the 34% ROI a sale would have cleared. Updated `data/inventory.json` (id 19) and `strategy/rams-2026-season.md`.
- **Chiefs, Cowboys, Seahawks-single sub-30%-ROI targets: accepted as-is.** These are confirmed capital-preservation sells (22%, 20%, ~20% ROI), not renegotiated upward. Updated notes in both `inventory.json` and `strategy/rams-2026-season.md` to reflect this is a settled decision, not an open flag.
- **Bruno Mars Oct 7 floor (James co-ownership): settled.** James keeps/attends his 2 of 6, no resale, no proceeds split needed. Rewrote `strategy/bruno-mars-oct-7.md` — this position is now full ATTEND for both parties, category stays CLIENT only to keep the co-ownership accounting flagged.
- **Sold/active split: done.** Moved the 7 closed positions (YE ×4, Monster Jam, Bruno Mars Sep 30 Row 7, R&B Sep 25 C134) out of `data/inventory.json` into `data/sold.json`, matching the CLAUDE.md file convention. `inventory.json` now holds only the 19 still-active positions.

### Current portfolio state
**19 active positions** — SELL: 10 · ATTEND: 5 · CLIENT: 3 · KEEP: 1
**7 closed positions** in `data/sold.json` — $700.30 + $275.75 + $159 realized profit across YE/Monster Jam/Bruno/R&B (see individual strategy files for full breakdowns).

Rams season updated projection: **+$1,082** across 4 remaining SELL positions (Packers $450, Chiefs $262, Cowboys $286, Seahawks-single $84) if all hit target, plus $556 from Saints company billing.

### Cleanup
- Removed stale `data/strategies/*.md` (7 files) from session 1 — fully superseded by the new `strategy/` directory.

### What's still open
- `sofi-ticket-manager.jsx` still sits unused at project root. Candidate for deletion once we're confident nothing else needs it as reference — not removed yet since it wasn't explicitly in scope this session.
- Dashboard build (`dashboard/`) — deferred by design this session, needs its own dedicated session.
- No new data-quality issues surfaced this session; all positions have consistent SELL/ATTEND/CLIENT/KEEP tags and status values.

### What's next (session 4)
- Scope and build the dashboard reader against the now-stable `data/inventory.json` / `data/sold.json` split.
- Decide on `sofi-ticket-manager.jsx` — delete or keep as reference.
